import { StubView } from '../StubView';

// PHASE-2 STUB (Group D). Replace this whole component with the real Usuarios view; fill its
// i18n keys in i18n/wsUsuarios.ts (prefix "wsusr."). Data is ready on `useAdmin().service`.
export function Usuarios() {
  return <StubView titleKey="wsusr.title" subtitleKey="wsusr.subtitle" />;
}
