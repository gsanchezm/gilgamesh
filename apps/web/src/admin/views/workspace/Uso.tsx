import { StubView } from '../StubView';

// PHASE-2 STUB (Group D). Replace this whole component with the real Uso view; fill its
// i18n keys in i18n/wsUso.ts (prefix "wsu."). Data is ready on `useAdmin().service`.
export function Uso() {
  return <StubView titleKey="wsu.title" subtitleKey="wsu.subtitle" />;
}
