import { StubView } from '../StubView';

// PHASE-2 STUB (Group D). Replace this whole component with the real Ajustes view; fill its
// i18n keys in i18n/wsAjustes.ts (prefix "wsa."). Data is ready on `useAdmin().service`.
export function Ajustes() {
  return <StubView titleKey="wsa.title" subtitleKey="wsa.subtitle" />;
}
