import { StubView } from '../StubView';

// PHASE-2 STUB (Group D). Replace this whole component with the real Proyectos view; fill its
// i18n keys in i18n/wsProyectos.ts (prefix "wsp."). Data is ready on `useAdmin().service`.
export function Proyectos() {
  return <StubView titleKey="wsp.title" subtitleKey="wsp.subtitle" />;
}
