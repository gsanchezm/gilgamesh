import { StubView } from '../StubView';

// PHASE-2 STUB (Group D). Replace this whole component with the real Facturacion view; fill its
// i18n keys in i18n/wsFacturacion.ts (prefix "wsf."). Data is ready on `useAdmin().service`.
export function Facturacion() {
  return <StubView titleKey="wsf.title" subtitleKey="wsf.subtitle" />;
}
