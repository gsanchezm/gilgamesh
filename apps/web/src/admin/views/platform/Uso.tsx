import { StubView } from '../StubView';

// PHASE-2 STUB (Group B). Replace this whole component with the real Uso view; fill its
// i18n keys in i18n/uso.ts (prefix "uso."). Data is ready on `useAdmin().service`.
export function Uso() {
  return <StubView titleKey="uso.title" subtitleKey="uso.subtitle" />;
}
