import { StubView } from '../StubView';

// PHASE-2 STUB (Group B). Replace this whole component with the real Ingresos view; fill its
// i18n keys in i18n/ingresos.ts (prefix "ingresos."). Data is ready on `useAdmin().service`.
export function Ingresos() {
  return <StubView titleKey="ingresos.title" subtitleKey="ingresos.subtitle" />;
}
