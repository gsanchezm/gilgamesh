import { StubView } from '../StubView';

// PHASE-2 STUB (Group C). Replace this whole component with the real Auditoria view; fill its
// i18n keys in i18n/auditoria.ts (prefix "auditoria."). Data is ready on `useAdmin().service`.
export function Auditoria() {
  return <StubView titleKey="auditoria.title" subtitleKey="auditoria.subtitle" />;
}
