import { StubView } from '../StubView';

// PHASE-2 STUB (Group B). Replace this whole component with the real Salud view; fill its
// i18n keys in i18n/salud.ts (prefix "salud."). Data is ready on `useAdmin().service`.
export function Salud() {
  return <StubView titleKey="salud.title" subtitleKey="salud.subtitle" />;
}
