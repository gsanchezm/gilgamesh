import { StubView } from '../StubView';

// PHASE-2 STUB (Group A). Replace this whole component with the real ClienteDetalle view; fill its
// i18n keys in i18n/clienteDetalle.ts (prefix "cd."). Data is ready on `useAdmin().service`.
export function ClienteDetalle() {
  return <StubView titleKey="cd.title" subtitleKey="cd.subtitle" />;
}
