import { StubView } from '../StubView';

// PHASE-2 STUB (Group C). Replace this whole component with the real Usuarios view; fill its
// i18n keys in i18n/usuarios.ts (prefix "usuarios."). Data is ready on `useAdmin().service`.
export function Usuarios() {
  return <StubView titleKey="usuarios.title" subtitleKey="usuarios.subtitle" />;
}
