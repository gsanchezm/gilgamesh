import { StubView } from '../StubView';

// PHASE-2 STUB (Group A). Replace this whole component with the real ProyectoDetalle view; fill its
// i18n keys in i18n/proyectoDetalle.ts (prefix "pd."). Data is ready on `useAdmin().service`.
export function ProyectoDetalle() {
  return <StubView titleKey="pd.title" subtitleKey="pd.subtitle" />;
}
