import { StubView } from '../StubView';

// PHASE-2 STUB (Group B). Replace this whole component with the real Planes view; fill its
// i18n keys in i18n/planes.ts (prefix "planes."). Data is ready on `useAdmin().service`.
export function Planes() {
  return <StubView titleKey="planes.title" subtitleKey="planes.subtitle" />;
}
