import { StubView } from '../StubView';

// PHASE-2 STUB (Group A). Replace this whole component with the real Clientes view; fill its
// i18n keys in i18n/clientes.ts (prefix "clientes."). Data is ready on `useAdmin().service`.
export function Clientes() {
  return <StubView titleKey="clientes.title" subtitleKey="clientes.subtitle" />;
}
