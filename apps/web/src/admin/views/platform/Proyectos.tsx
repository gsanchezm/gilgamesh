import { StubView } from '../StubView';

// PHASE-2 STUB (Group A). Replace this whole component with the real Proyectos view; fill its
// i18n keys in i18n/proyectos.ts (prefix "proyectos."). Data is ready on `useAdmin().service`.
export function Proyectos() {
  return <StubView titleKey="proyectos.title" subtitleKey="proyectos.subtitle" />;
}
